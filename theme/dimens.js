import normalize from '../utils/ResponsiveTextUtils';

const Dimens = {
    TITLE_SIZE_BIG : normalize(17),
    TITLE_SIZE_MEDIUM : normalize(14),
    TITLE_SIZE_SMALL : normalize(11),
    TEXT_NORMAL: normalize(10),
    PADDINGHORIZONTAL: normalize(20),
}

export default Dimens